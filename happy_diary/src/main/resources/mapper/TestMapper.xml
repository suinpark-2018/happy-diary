<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.happydiary.dao.TestDao">
    <!-- 현재 시간 반환하는 SQL 문 -->
    <select id="now" resultType="String">
        SELECT NOW()
    </select>
</mapper>